#!/usr/bin/env perl

use strict;
use warnings;
use utf8;
use FindBin;
use lib "$FindBin::Bin";
use Genoring::GenoringTool;

my $program = new Genoring::GenoringTool('gff3sort.pl');
$program->run();
print $program->getResponse();
