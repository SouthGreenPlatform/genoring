#!/usr/bin/env perl

use strict;
use warnings;
use utf8;
use FindBin;
use lib "$FindBin::Bin";
use Genoring::GenoringTool;

my $program = new Genoring::GenoringTool('gt');
$program->run();
print $program->getResponse();
