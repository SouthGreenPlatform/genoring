name: "SSL"
description: "Provides SSL certificat support for HTTPS."
version: 1.0
tags:
 - ssl
 - https
 - drupal
genoring_script_version: 1.0-alpha1
dependencies:
  services:
    - "REQUIRES genoring genoring"
    - "REQUIRES genoring genoring-proxy"
