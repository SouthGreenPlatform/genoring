name: "JBrowse2"
description: "JBrowse2 is a genome browser that runs on the web."
version: 1.0
services:
  genoring-jbrowse2:
    name: "JBrowse2"
    description: "The JBrowse2 web application."
    version: 1.0
dependencies:
  volumes:
    - "requires genoring genoring-data-volume"
