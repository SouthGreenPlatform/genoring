# v1.0
# image: jbrowse
image: genoring-jbrowse
# image: jbrowse/gmod-jbrowse

env_file:
  - ${PWD}/modules/jbrowse/env/jbrowse.env
ports:
  - 8125:80
restart: unless-stopped
volumes:
  - genoring-data-volume:/data
