image: genoring-jbrowse
build: ${PWD}/modules/jbrowse/src/genoring-jbrowse
pull_policy: never
profiles: ["backend"]
env_file:
  - ${PWD}/env/jbrowse_jbrowse.env
ports:
  - 8125:80
restart: unless-stopped
volumes:
  - genoring-data-volume:/data
