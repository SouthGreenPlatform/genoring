image: genoring-jbrowse
build: ${GENORING_DIR}/modules/jbrowse/src/genoring-jbrowse
pull_policy: never
# @todo See how to switch to a backned only docker container only for loading
# tasks.
# profiles: ["backend"]
env_file:
  - ${PWD}/env/jbrowse_jbrowse.env
ports:
  - 8125:80
restart: unless-stopped
volumes:
  - ${GENORING_VOLUMES_DIR}/jbrowse/data:/jbrowse/data
  - genoring-data-volume:/data
