name: "BrAPI Mapper"
description: "Provides Breeding API services (v1 and v2) and an interface to map data to those services."
version: 1.0
tags:
 - brapip
 - breeding
 - service
genoring_script_version: 1.0-alpha1
dependencies:
  services:
    - "REQUIRES genoring genoring"
    - "REQUIRES genoring genoring-proxy"
